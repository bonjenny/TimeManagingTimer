---
title: Version 0.10.5 Release Notes
version: 0.10.5
date: 2026-01-27
step: Dark Mode Color Enhancement
---

# v0.10.5 릴리즈 노트

## 🚀 새로운 기능

### 다크모드 테마색 자동 보정

다크모드에서 테마색이 **너무 어두워서 보이지 않는 문제**를 해결했습니다.

#### 주요 개선사항

| 항목 | 설명 |
|---|---|
| **자동 밝기 보정** | 밝기 45% 미만의 어두운 색상을 자동으로 밝게 조정 |
| **색조 유지** | 원본 색상의 색조(Hue)는 그대로 유지하면서 밝기만 보정 |
| **일관된 적용** | 모든 테마색 사용처에서 동일하게 보정된 색상 표시 |

#### 보정이 적용되는 곳

1. **설정 페이지** - 팔레트 미리보기, 대표 색상 표시, 커스텀 팔레트 색상
2. **간트 차트** - 작업 막대 색상, 프로젝트 칩 색상
3. **프리셋 패널** - 프리셋 항목 색상
4. **MUI 컴포넌트** - 탭 버튼 선택 상태, 버튼 색상, 포커스 색상

---

## 🔧 기술적 변경사항

### 신규 함수

| 함수 | 파일 | 설명 |
|---|---|---|
| `getAdjustedPalette()` | `colorPalette.ts` | 다크모드에서 보정된 팔레트 배열 반환 |
| `getAdjustedColor()` | `colorPalette.ts` | 다크모드에서 보정된 단일 색상 반환 |

### 수정 파일

| 파일 | 변경 내용 |
|---|---|
| `src/utils/colorPalette.ts` | `getAdjustedPalette`, `getAdjustedColor` 함수 추가 |
| `src/components/pages/SettingsPage.tsx` | 팔레트 미리보기에 다크모드 보정 적용 |
| `src/components/gantt/GanttChart.tsx` | 작업 색상에 다크모드 보정 적용 |
| `src/components/preset/PresetPanel.tsx` | 프리셋 색상에 다크모드 보정 적용 |
| `src/App.tsx` | MUI 테마 생성 시 보정된 색상 사용 |

### 신규 테스트

| 파일 | 테스트 항목 |
|---|---|
| `colorPalette.test.ts` | `getAdjustedPalette` 6개, `getAdjustedColor` 5개 테스트 추가 |

---

## 📊 색상 보정 알고리즘

```typescript
// 다크모드에서 색상 밝기 자동 보정
// 색상이 너무 어두우면 같은 색조(hue)를 유지하면서 밝기를 높임

const adjustColorForDarkMode = (hex, isDark, minLightness = 45) => {
  if (!isDark) return hex;
  
  const { h, s, l } = hexToHsl(hex);
  
  if (l < minLightness) {
    // 채도도 약간 높여서 색감 유지
    const adjustedSaturation = Math.min(s + 10, 100);
    return hslToHex(h, adjustedSaturation, minLightness);
  }
  
  return hex;
};
```

### 보정 예시

| 원본 색상 | 원본 밝기 | 보정 후 색상 | 보정 후 밝기 |
|---|---|---|---|
| `#264653` (네이비) | 25% | `#3d8799` | 45% |
| `#2d6a4f` (포레스트) | 30% | `#3d9e73` | 45% |
| `#e9c46a` (노랑) | 66% | (변경 없음) | 66% |

---

## 📝 업데이트 방법

- 웹 페이지를 새로고침하면 자동 적용됩니다.
- 다크모드 전환 시 색상이 자동으로 보정됩니다.
