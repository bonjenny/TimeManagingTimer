---
title: Version 0.11.0 Release Notes
version: 0.11.0
date: 2026-01-27
step: TimePicker & Inline Time Edit
---

# v0.11.0 릴리즈 노트

## 🚀 새로운 기능

### 1. MUI TimePicker 적용

설정 페이지의 점심시간 입력에 **MUI TimePicker**를 적용하여 더 나은 사용자 경험을 제공합니다.

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| **시간 입력 방식** | 브라우저 기본 time input | MUI TimePicker 위젯 |
| **시간 선택 UI** | 브라우저마다 다름 | 일관된 시계 UI |
| **패키지** | - | `@mui/x-date-pickers` 추가 |

### 2. 인라인 시간 편집 기능

작업 세션 상세 테이블에서 **시작/종료 시간을 클릭하면 바로 수정**할 수 있습니다.

#### 주요 특징

| 기능 | 설명 |
|---|---|
| **인라인 편집** | 시간 셀 클릭 시 즉시 input으로 전환 |
| **키보드 지원** | Enter로 저장, Escape로 취소 |
| **충돌 자동 조정** | 종료시간이 다음 세션과 겹치면 다음 세션 시작시간 자동 조정 |
| **역방향 조정** | 시작시간이 이전 세션과 겹치면 이전 세션 종료시간 자동 조정 |

### 3. 건의사항 모달 패딩 개선

Q&A 모달의 내부 패딩을 반응형으로 개선하여 가독성을 높였습니다.

| 화면 크기 | 패딩 |
|---|---|
| xs (모바일) | 16px |
| sm (태블릿) | 24px |
| md (데스크톱) | 32px |

---

## 🔧 기술적 변경사항

### 신규 패키지

| 패키지 | 버전 | 용도 |
|---|---|---|
| `@mui/x-date-pickers` | ^8.26.0 | TimePicker 컴포넌트 |

### 수정 파일

| 파일 | 변경 내용 |
|---|---|
| `src/components/pages/SettingsPage.tsx` | LocalizationProvider, TimePicker 적용 |
| `src/components/timer/TimerList.tsx` | 인라인 시간 편집 기능 추가 |
| `src/components/Layout.tsx` | 건의사항 모달 반응형 패딩 적용 |

### 신규 상태 및 함수 (TimerList.tsx)

| 이름 | 타입 | 설명 |
|---|---|---|
| `editingSessionTime` | state | 현재 편집 중인 세션 정보 (id, field) |
| `inlineTimeValue` | state | 인라인 편집 중인 시간값 |
| `startInlineTimeEdit()` | function | 시간 인라인 편집 시작 |
| `saveInlineTime()` | function | 시간 저장 및 충돌 자동 조정 |
| `cancelInlineTimeEdit()` | function | 편집 취소 |

---

## 📊 충돌 자동 조정 알고리즘

```typescript
// 종료 시간 변경 시 다음 세션과 충돌 체크
if (nextSession && newTimestamp > nextSession.startTime) {
  // 다음 세션의 시작 시간을 현재 종료 시간으로 조정
  updateLog(nextSession.id, { startTime: newTimestamp });
}

// 시작 시간 변경 시 이전 세션과 충돌 체크
if (prevSession && prevSession.endTime && newTimestamp < prevSession.endTime) {
  // 이전 세션의 종료 시간을 현재 시작 시간으로 조정
  updateLog(prevSession.id, { endTime: newTimestamp });
}
```

---

## 📝 업데이트 방법

- 웹 페이지를 새로고침하면 자동 적용됩니다.
- 세션 상세 테이블에서 시작/종료 시간을 클릭하여 바로 수정할 수 있습니다.

---

## 다음 버전 예정

- 주간 일정 개선
- 통계 및 리포트 기능 확장
